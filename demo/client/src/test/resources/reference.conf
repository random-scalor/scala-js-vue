
carrot.tool.build.scala {

    # scala build manager
    manager {
        # reaction actor:
        reactor-settle-delay = 500 ms # process action delay
    }
    
    # scala/js linker manager
    manager-js {
    
    }
    
}
